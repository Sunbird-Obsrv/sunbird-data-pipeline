include "base-test.conf"

kafka {
  input.topic = "flink.telemetry.ingestion"
  output.success.topic = "flink.telemetry.ingest"
  groupId = "flink-telemetry-processor-group"
}

task {
  consumer.parallelism = 1
  downstream.operators.parallelism = 1
}