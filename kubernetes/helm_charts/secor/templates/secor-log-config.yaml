apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ $.Release.Name }}-log-config
  namespace: {{ $.Values.secor_namespace }}
  labels:
    app: {{ $.Release.Name }}-log-config
    chart: {{ $.Chart.Name }}-{{ $.Chart.Version }}
    release: secor-log-config
    heritage: secor-log-config
data:
{{ ($.Files.Glob "config/log4j*.properties").AsConfig | nindent 4 }}