- hosts: local
  become: yes
  vars_files:
    - "{{inventory_dir}}/secrets.yml"
  roles:
    - oci-bds-spark-cluster
  tags:
    - copy-script
    
- hosts: bds-livy-node
  become: yes
  gather_facts: no
  vars_files:
    - "{{inventory_dir}}/secrets.yml"
  roles:
    - provision-oci-spark-cluster
  tags:
    - spark-provision
