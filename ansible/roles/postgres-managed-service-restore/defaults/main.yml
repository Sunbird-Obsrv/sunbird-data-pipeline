postgresql_restore_dir: /tmp/postgres-restore

db:
  name: ['analytics']
  role: ['analytics']
  user: ['azure_superuser, analytics']

#these variables are passed as extra vars
postgres_backup_filename:
postgres_user: 
postgres_password:
postgres_hostname: 

cloud_storage_dppostgresqlbackup_bucketname: "{{ cloud_storage_management_bucketname }}"
cloud_storage_dppostgresqlbackup_foldername: dp-postgresql-backup
