---
- name: Download the installation file
  get_url:
    url: "{{ oci_cli_url }}"
    dest: /tmp/ocicli.zip

- name: Installing unzip
  apt:
    name: "{{item}}"
    state: latest
  with_items:
    - zip
    - unzip

- name: Unzip the installer
  unarchive:
    src: /tmp/ocicli.zip
    dest: /tmp/
    remote_src: yes    

- name: install oci cli
  become: yes
  become_user: "{{ analytics_user }}"
  shell: ./oci-cli-installation/install.sh --accept-all-defaults
  args:
    chdir: /tmp/
