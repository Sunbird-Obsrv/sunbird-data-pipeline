postgresql_restore_dir: /tmp/postgres-restore
postgres_backup_azure_container_name: dp-postgresql-backup

db:
  name: ['analytics']
  role: ['analytics']

#these variables are passed as extra vars
postgres_backup_filename:
postgres_user: 
postgres_password:
postgres_hostname: 
