samza_slack_channel: samza-alrt-{{env}}
throughput_threshold_value: 1000
influxdb_query_duration: 60

max_consumer_lag_count: 10
consumer_lag_influxdb_query_duration: 30
consumer_lag_slack_channel: consumer_lag-alrt-{{env}}
consumer_lag_slack_webhook: "{{ samza_alert_slack_url }}"