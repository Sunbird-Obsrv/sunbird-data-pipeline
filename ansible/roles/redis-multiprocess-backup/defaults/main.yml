redis_backup_dir: /tmp/redis-backup
redis_data_dir: /data
redis:
  config:
    device:
      name: 'device'
    user:
      name: 'user'
    content:
      name: 'content'
    dialcode:
      name: 'dialcode'

cloud_storage_dpredisbackup_bucketname: "{{ cloud_storage_management_bucketname }}"
cloud_storage_dpredisbackup_foldername: dp-redis-backup
