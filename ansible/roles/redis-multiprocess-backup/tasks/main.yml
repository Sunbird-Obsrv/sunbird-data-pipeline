- name: Create the directory
  file: path={{ redis_backup_dir }}/{{ item }} state=directory recurse=yes
  with_items: "{{ redis.processes }}"

- set_fact:
    redis_backup_file_name: "redis-backup-{{ lookup('pipe', 'date +%Y-%m-%d-%T') }}-{{ ansible_hostname }}.zip"
 
- name: copy dump.rdb files
  copy:
    src: /home/analytics/redis-stable/dump-{{ item }}.rdb
    dest: "{{ redis_backup_dir }}/{{ item }}/dump-{{ item }}.rdb"
    remote_src: yes
  with_items: "{{ redis.processes }}"

- name: Create archive of backup directory
  archive: path="{{ redis_backup_dir }}/{{ item }}/*" dest="{{ redis_backup_dir }}/{{ item }}/{{ redis_backup_file_name }}" format=zip
  async: 3600
  poll: 10
  with_items: "{{ redis.processes }}"

- name: upload to azure
  include_role:
    name: artifacts-upload-azure
  vars:
    artifact: "{{ item }}/{{ redis_backup_file_name }}"
    artifact_path: "{{ redis_backup_dir }}/{{ item }}/{{ redis_backup_file_name }}"
    artifacts_container: "{{ redis_backup_azure_container_name }}"
  with_items: "{{ redis.processes }}"  
   
- name: clean up backup dir after upload
  file: path={{ redis_backup_dir }} state=absent
